apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http

---
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc-2
spec:
  selector:
    app: frontend-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http